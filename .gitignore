# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*
!.env.example

# vercel
.vercel

# claude code worktrees
.claude/worktrees/

# supabase cli scratch
supabase/.temp/

# case study intake and previews. Unpublished client-adjacent prose and a
# few hundred KB of generated art — none of it belongs in a diff.
/.case-study/

# Skills installed from the skills repo as directory junctions. The junction is
# a local install; the source of truth is github.com/RegainFlow/skills, and
# committing the resolved contents here would fork them silently.
# `no-ai-slop` is deliberately NOT ignored — it is authored in this repo.
/.claude/skills/publish-case-study/
/.claude/skills/regainflow-brand-guidelines/

.vercel
.env*
